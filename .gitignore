# Version control ignores

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.venv/
var/
.pytest_cache/
.coverage
htmlcov/
.ipynb_checkpoints/

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.eslintcache
.stylelintcache
dist/
dist-ssr/
*.tsbuildinfo

# Environment variables & secrets
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.pem
*.key

# Credentials pasted as documents. This repo is public.
*key*.pdf
*secret*.pdf

# Terraform. State records every resource attribute, including the plaintext
# value of any SecureString parameter it manages, so it must never be pushed;
# use a private encrypted S3 backend instead. .terraform/ is ~700MB of
# provider binaries and is reproducible with `terraform init`.
.terraform/
*.tfstate
*.tfstate.*
.terraform.tfstate.lock.info
# Saved plans embed the same resolved values as state, secrets included.
tfplan
tfplan.*
*.tfplan
*.tfvars

# Real bank statements. These contain live account numbers and transaction
# history for named individuals; only synthetic fixtures belong in Git.
Acct Statement*.pdf
*_unlocked.pdf
hardik*.pdf
avinash*.pdf

# Debugging scratch left at the repo root: captured API responses, AWS CLI
# output, one-off repro scripts and screenshots pasted during a session. Rooted
# with a leading slash so package.json, tsconfig.json and friends are untouched.
/an.json
/cf.json
/logs.json
/pr.json
/pre.json
/r.json
/s3.json
/ssmres.json
/sum.json
/tfout.json
/tx.json
/repro.py
/image*.png
frontend/ctx.txt
frontend/live.js
frontend/up.js
frontend/v.js

# OS metadata
.DS_Store
Thumbs.db

# Editor/IDE configs
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp

# Demo data - do not push
demo_data/
**/demo*.csv
**/hdfc_*.csv
**/sbi_*.csv
